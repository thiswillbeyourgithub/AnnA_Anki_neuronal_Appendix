plyer >= 2.0.0  # for notification, can help to install dbus-python too
pyfiglet >= 0.8.post1   # for banner
prompt_toolkit >= 3.0.14  # for autocomplete prompt
python_Levenshtein == 0.12.2  # to correct for field mapping and note type spelling errors
beepy>=1.0.7  # make a beeping sound
ankipandas>=0.3.10  # to load the anki collection
ftfy>=6.1.1

# datascience and computation
pandas == 1.2.3
joblib >= 1.1.0
umap-learn >= 0.5.3

numpy == 1.23.5
scikit-learn>=1.1.1
tqdm >= 4.62.3
tqdm_logger >= 1.0.1

nltk >= 3.5
tokenizers >= 0.10.3
transformers >= 4.22.2
sentence_transformers >= 2.2.2
tables >= 0.0.4

# 2D plot
matplotlib>=3.6.1
networkx>=2.8.7
pydot>=1.4.2
